# 物料出入库管理系统 — AI 辅助产品开发实录

> **记录日期**：2026-06-10 至 2026-06-12  
> **项目代号**：stock-test  
> **文档性质**：开发过程记录 / 分享案例素材  
> **协作方式**：产品负责人 × Cursor AI Agent（多轮对话迭代）  
> **Wiki 同步**：已追加至 [Wiki 原文档](https://zcnce50wan15.feishu.cn/wiki/Nzw3wX2USiIjtckGQbVckSQRnFf)（revision 224），并同步至 [物料出入库管理系统 — AI 辅助产品开发实录 - 飞书云文档](https://zcnce50wan15.feishu.cn/wiki/UeJrwo3EDiDRcpk6PMBc7Wq3nJh)

---

## 一、案例背景：我们要解决什么问题？

机器人研发实验室需要管理分散在货柜、货架、专用架上的硬件物料（电机、传感器、算力板、线缆等）。现状痛点：

- 找物料靠个人记忆，效率低
- 领用、入库缺少标准记录，难以追溯
- 快递到货后暂存与正式上架流程不清晰
- 库存数量靠人工记账，易出错

**团队特征**：小团队、专人维护、低访问量（每天几十笔操作以内），已在飞书协作。

**产品目标（一句话）**：记清楚、找得到、可追溯 —— 不是做 ERP，不是高并发 WMS。

---

## 二、起点：飞书 Wiki 上的初稿设计

对话从一份飞书知识库文档开始：

- 文档：[物料出入库管理系统产品设计文档（Wiki 初稿）](https://zcnce50wan15.feishu.cn/wiki/Nzw3wX2USiIjtckGQbVckSQRnFf)

初稿提出的技术方向：

| 维度 | 初稿方案 |
|------|---------|
| 数据库 | 飞书多维表格（Bitable） |
| 应用形态 | 飞书内嵌 Web App |
| 前端 | React |
| 后端 | Python + FastAPI |

业务模块：分类管理、库位管理、出入库、库存管理、物料查找。

附图明确了 **12 类机器人物料分类**（电机模组、感知设备、算力设备等）及存放位置约定（货柜/货架/快递暂存区等）。

---

## 三、对话演进全记录（决策是如何一步步形成的）

### 阶段 1：方案是否合适？怎么实现？（2026-06-10）

**提问**：参照 Wiki 初稿做系统，是否合适？具体怎么实现？

**AI 动作**：

- 阅读飞书 Wiki 原文与分类附图
- 评估「Bitable + 飞书应用」与团队场景的匹配度

**结论**：

- ✅ 对实验室小团队 **合适**
- MVP 应做轻量：搜索、出入库、库存、流水
- 暂不做：高并发、审批流、独立数据库、复杂 ERP

**初版技术倾向**：飞书官方库存模板 + 多维表格「应用模式」（零代码最快）。

---

### 阶段 2：场景澄清 —— 低访问量、专人维护（2026-06-10）

**用户补充**：没有大量访问，专人维护，不需要很复杂。

**决策变化**：

| 原考虑 | 调整后 |
|--------|--------|
| Redis 缓存 | ❌ 不需要 |
| 乐观锁防超卖 | ❌ 不需要 |
| 复杂审批 | ❌ 不需要 |
| 自研全栈 | 可大幅简化 |

**沉淀原则**：够用就好；80% 价值在「搜 + 出库 + 入库」。

---

### 阶段 3：调研成熟方案 —— 能否直接拿来用？（2026-06-10）

**提问**：有没有已做过的类似方案，直接拿来用？

**AI 调研范围**：

| 方案 | 结论 |
|------|------|
| 飞书官方模板 + 应用模式 | 最省事，但无独立 H5 |
| [Erniu 开源](https://github.com/dyue708/Erniu-Inventory-management-AI-agent) | 唯一 Bitable+出入库成品，机器人/卡片形态 |
| [lark-samples](https://github.com/larksuite/lark-samples) | 官方网页应用脚手架（免登+JSAPI） |
| Teable / InvenTree / 若依WMS | 成熟但偏离飞书 Bitable 生态 |

**关键发现**：不存在「Bitable + 工作台 H5 + 实验室物料 + 三角色」的开箱即用单一开源项目。

---

### 阶段 4：建立项目文档体系（2026-06-10）

**用户要求**：产生产品设计文档和架构设计文档，随讨论持续更新。

**产出**（本地仓库 `stock-test/docs/`）：

- `产品设计文档.md`（PRD，现 v0.8）
- `架构设计文档.md`（现 v0.8）

文档采用 **变更记录表**，每次决策可追溯版本。

---

### 阶段 5：权限管理专题设计（2026-06-10）

**提问**：系统具体权限如何设计？

**方案**：三层权限模型

1. **入口层**：飞书群组 + Bitable 协作者 + 工作台可见范围
2. **功能层**：H5 页面显隐 + API 鉴权
3. **数据层**：Bitable 高级权限（库存字段只读）

**三角色**：

| 角色 | 入库 | 出库 | 维护主数据 |
|------|:----:|:----:|:----------:|
| USER（研发） | ❌ | ✅ | ❌ |
| KEEPER（库管） | ✅ | ✅ | ✅ |
| ADMIN | ✅ | ✅ | ✅ |

---

### 阶段 6：产品认知 —— 功能与场景科普（2026-06-10）

**提问**：不知道怎么设计产品，应该做哪些功能？一般使用场景？

**沉淀内容**（写入 PRD §4）：

- 7 个典型场景：领用、采购入库、快递暂存、新人查找、归还、盘点、低库存
- 功能三层：必须有 / 建议有 / 以后再说
- 产品设计六步法

**用户反馈**：「让你记到文档里，为啥不记」→ 确立 **文档先行、对话即落盘** 的工作方式。

---

### 阶段 7：固化为项目强制规则（2026-06-10）

**用户要求**：把规范作为项目规则，以后开发必须遵守。

**产出**：

- `.cursor/rules/stock-project-conventions.mdc`（Cursor 持久规则，`alwaysApply: true`）
- `AGENTS.md`（人机协作文档入口）

---

### 阶段 8：MVP 技术路线明确 —— Bitable + 飞书网页应用（2026-06-10）

**用户明确**：MVP = 多维表格作数据库 + [飞书网页应用嵌入工作台](https://open.feishu.cn/document/embed-web-app-into-feishu-workbench/introduction)。

**决策变化**：

| 之前倾向 | 最终 MVP |
|---------|---------|
| 应用模式为主界面 | **H5 网页应用**为主界面 |
| MVP 不做前后端 | **轻量 FastAPI + React H5** 纳入 MVP |
| Bitable 自动化改库存 | **后端 API** 写流水并更新库存 |

应用模式降级为 **库管备用入口**。

---

### 阶段 9：部署环境 —— 内网还是公网？（2026-06-10）

**提问**：内网咋搞？可以后面再说吗？是否影响开发？

**结论**：

- ✅ **可以后置**，不阻塞 P0/P1 开发
- 开发期：本机 + lark-samples 跑通；联调可用隧道
- 上线前：需确定 HTTPS 主页 URL 配置到飞书工作台

---

### 阶段 10：反造轮子 —— 组合方案定稿（2026-06-10）

**提问**：是否在重复造轮子？有没有更好方案？

**最终方案（已决）**：

```
Erniu/官方 Bitable 模板  +  lark-samples 网页应用  +  自研薄层（4 H5 页 + 8 API）
```

| 层 | 拿来用 | 自研 |
|----|--------|------|
| 数据 | Erniu / 官方模板 fork | 按 PRD 改五表字段 |
| 飞书接入 | lark-samples 免登+JSAPI | 配置工作台 |
| 业务 | 参考 Erniu 出入库逻辑 | 8 个 API + 角色校验 |
| 界面 | 官方示例骨架 | 搜索/出库/入库/详情 4 页 |

**工期**：组合方案约 **1 周**（全从零约 2～3 周）。

---

### 阶段 11：环境需求整理 —— 开发者上手清单（2026-06-11）

**用户提问**：产品、架构、权限都确定了，但「开发者拿到项目后具体怎么准备环境」这件事还很模糊。能否整理一份详细的「环境配置 + 依赖安装 + 部署方案」文档，让开发者一次性明白要装什么、怎么装？

**AI 动作**：

1. 从产品设计文档 §3.8、架构设计文档 §3.4 / §7 / §8、AGENTS.md 提炼环境需求
2. 区分四大环境：飞书企业 / 开放平台 / Bitable / 开发者本机
3. 按阶段明确：P0（数据+应用就绪）/ P1（开发+联调）/ 上线前（部署）
4. 补充具体命令：系统包管理 + pip + npm
5. 创建「§12 安装与下载总汇」汇总所有命令

**结论与产出**：

新增 `docs/环境需求.md` v1.1，13 章 + 1 总汇：

| 章节 | 内容 | 亮点 |
|------|------|------|
| 1～3 | 文档说明 + 环境总览 + 分阶段需求 | P0/P1/上线前清单对齐实施节奏 |
| 4 + 命令 | 开发者本机 | **新增系统命令**（winget / brew / apt） |
| 5 + 命令 | Python 依赖 | **新增 pip + venv 完整流程** |
| 6 + 命令 | Node.js 依赖 | **新增 npm 完整流程** |
| 7～11 | 飞书 / Bitable / 部署 | 配置指南 |
| **12** | **安装总汇** | **汇总所有命令，一键复制** |
| 13 | 检查清单 | 验收对标 |


**关键决策**：

| 是否阻塞？ | 答案 | 依据 |
|----------|------|------|
| 环境部署 | ❌ 否，可后置 | 架构文档 §7.5 |
| 与架构文档重复？ | ❌ 不重复 | 架构讲「做什么」，环境讲「怎么做」 |


---

### 阶段 12：文档细化与日程同步（2026-06-11，当日追加）

**背景**：继续推进文档与日程一致性，并让设计文档可直接驱动 AI 生成代码骨架。

**主要工作项**：

- 将 `docs/技术准备与开发日程.md` 精简并按要求调整为不在文档中提及“周末休息”，同时把含周末的日程移动为工作日，确保日程实际避开周末（日期统一、顺延并同步相关里程碑）。
- 新建 `docs/总体设计.md`（v1.0），包含架构概览、详细组件职责、API 列表、非功能与部署建议，并在文件末尾加入用于 AI 自动生成工程骨架的提示词。
- 新建 `docs/模块设计.md`（v1.0），对后端/前端模块做逐项拆解，补充每个模块的功能职责、接口签名、输入/输出示例、幂等与并发注意点、测试策略及模块级 AI 提示词。
- 在 `docs/总体设计.md` 中临时添加过两张 SVG 矢量图用于说明（架构图、数据模型），随后按用户要求将矢量图移除并删除对应 SVG 文件，文档中仅保留文字说明。
- 对 `docs/技术准备与开发日程.md`、`docs/总体设计.md`、`docs/模块设计.md` 等文件做多轮编辑，使日程、里程碑、测试节点、交付时间等在文档中一致。

**变更文件列表（当日）**：

- 新增：`docs/总体设计.md`、`docs/模块设计.md`
- 更新：`docs/技术准备与开发日程.md`（日期顺延、删除周末提示）、`docs/总体设计.md`（扩充前置说明与组件职责）、`docs/模块设计.md`（扩充模块功能说明）
- 新增后删除（用户要求撤回）：`docs/images/architecture.svg`、`docs/images/data_model.svg`（已删除）

**产出要点**：

- 文档层面：两份详细设计文档可直接作为 AI 代码生成的输入（包含明确的文件结构、接口合约与示例）。
- 日程层面：所有文档已统一为避开周末的连续工作日安排，且不在文档中声明“周末休息”。
- 可复现性：我在文档中加入了运行/安装命令示例与测试要点，方便开发者按步骤复现环境。

**后续建议**：

- 将上述变更提交为一次 Git commit（我可代为提交并给出 commit message）。
- 如需，使用文档末尾的 AI 提示词，自动生成 `backend/` 与 `frontend/` 的最小可运行骨架，并附带示例测试。

---

### 阶段 13：MVP 代码落地 —— 飞书免登 + Bitable 真实读写（2026-06-11～12）

**背景**：P1 骨架已在 mock 模式跑通；用户填入飞书凭证与 Bitable 表 ID 后，要求本地/联调环境能读写真实数据并完成出入库。

**AI 动作**：

1. 迁入 lark-samples 思路，实现 `POST /auth/feishu/login`、`GET /auth/jsapi-config`、Bearer token 会话
2. 群组角色：通过 `user_access_token` + `is_in_chat` 判定 ADMIN / KEEPER / USER；补充 IM 权限 `im:chat.members:read`
3. 实现 `BitableRepository`（real 模式），对齐 Erniu 模板字段名（`物料ID`、`库位ID`、`库存数量` 等）
4. 修复 link 字段解析（`record_ids`）、流水类型 `in`/`out`、操作人 `[{"id": "ou_xxx"}]`、`user_id_type` 传参
5. 修复入库 500：补 `list_locations` 方法
6. 前端 UI 迭代：`GET /materials/catalog?stock_only=...` 一次拉取物料+库存；出库/入库页改为表单+选择器

**结论**：

| 项 | 状态 |
|----|------|
| 本地 `BITABLE_MODE=real` 出入库写表 | ✅ 已验证 |
| 飞书群组角色判定 | ✅ 已验证（需 IM 权限） |
| Mock 测试 | ⚠️ `test_api.py` 在 real 模式下需 mock 环境或拆分 |

**关键代码路径**：

- `backend/app/services/feishu_client.py` — 免登、JSAPI、群组成员
- `backend/app/bitable/repository.py` — 五表读写、出入库事务
- `backend/app/bitable/fields.py` — Erniu 字段映射
- `frontend/src/pages/Outbound.tsx`、`Inbound.tsx` — 业务页重构
- `feishu_permission_import.json` — 权限导入模板更新

---

### 阶段 14：生产服务器首次部署（2026-06-12）

**背景**：用户将应用部署至阿里云 ECS `stock@182.92.122.77`（Alibaba Cloud Linux 3），目标：Nginx 托管前端 + systemd 跑 uvicorn，供飞书工作台访问。

**环境特征**：

| 项 | 说明 |
|----|------|
| 系统 | Alibaba Cloud Linux 3（`dnf`，非 Ubuntu `apt`） |
| Python | 3.11 venv，`pip install -r requirements.txt` |
| 后端 | `uvicorn` 监听 `127.0.0.1:8000`，`stock-flow.service` |
| 前端 | `npm run build` → `frontend/dist` |
| 反向代理 | Nginx：`/api/` → 8000，静态文件 serve `dist` |

**踩坑记录**：

1. **`.env` 上传**：须在 **Windows PowerShell** 用 `scp` 上传；在 SSH 会话里写 `C:\...` 会报 `Could not resolve hostname c`
2. **早期 `/health` 显示 mock**：服务器 `.env` 未同步或未重启 service
3. **无自有域名**：`https://fr-stock-flow` 无 TLD，不可用于飞书；不能对 ngrok 域名跑 certbot

**部署架构（初版）**：

```text
公网 / ngrok HTTPS
    → Nginx :80
        → /api/  → uvicorn :8000
        → /      → frontend/dist
```

---

### 阶段 15：HTTPS 与 ngrok 方案澄清（2026-06-12）

**提问**：没有域名，飞书要求 HTTPS，怎么配？

**结论**：

| 方案 | 决策 |
|------|------|
| 购买域名 + certbot | ❌ 暂不采用 |
| 服务器本机 ngrok 穿透 | ✅ 采用：`ngrok http <端口>` 得 `https://xxxx.ngrok-free.dev` |
| 飞书三处 URL | 主页、重定向 URL、H5 可信域名 **必须一致** |
| `backend/.env` | `FEISHU_REDIRECT_URI`、`CORS_ORIGINS` 同步 ngrok 域名 |

**ngrok 额外踩坑**：

- 服务器存在 `http_proxy` 等环境变量 → 免费 ngrok 报 **ERR_NGROK_9009**；需 `unset http_proxy https_proxy ...` 并检查 `~/.config/ngrok/ngrok.yml` 无 `proxy_url`
- 从 AWS S3 下载 ngrok 包失败（非 gzip）→ 可改用 `npm install -g ngrok` 或从本机 scp 安装包

**备选**：若 ngrok 持续受代理限制，可换 **cloudflared** 隧道。

---

### 阶段 16：AutoDL Flow 占 80 端口 —— 打开应用却是登录页（2026-06-12）

**现象**：启动/访问应用时出现 **「AutoDL Flow 登录界面」**；ngrok 日志出现 `GET /login`。

**排查过程**：

```bash
sudo ss -tlnp | grep -E ':80|:8080'
curl -I http://127.0.0.1:80
# → HTTP/1.1 302 FOUND, Location: /login

curl -s http://127.0.0.1/login | head -5
# → 中文 HTML 登录页（AutoDL Flow，非 stock-flow）
```

**根因**：

- 80 端口虽由 **Nginx** 监听，但 **default 站点仍是 AutoDL Flow**（302 → `/login`），不是 `stock-flow.conf`
- stock-flow **没有** `GET /login` 路由；飞书登录走 `POST /api/auth/feishu/login`
- 用户访问公网 IP 或 `ngrok http 80` 时，实际打开的是 AutoDL 平台 UI

**决策**：**不抢 AutoDL 的 80**，stock-flow 改监听 **8080**，ngrok 指向 8080。

---

### 阶段 17：8080 联调 —— API 已通、静态页待修（2026-06-12，当日进展）

**Nginx 调整**：

```nginx
server {
    listen 8080;
    root /home/stock/stock-flow/frontend/dist;
    location /api/ { proxy_pass http://127.0.0.1:8000/; }
    location / { try_files $uri $uri/ /index.html; }
}
```

**验证结果**：

| 请求 | 结果 | 含义 |
|------|------|------|
| `GET /api/health` | ✅ 200 JSON | 后端 + Bitable + 飞书配置正常 |
| `GET /` | ❌ 500 | Nginx 读静态文件失败（疑为 `/home/stock` 目录权限或 SELinux） |

**health 返回摘录（说明 real 模式已就绪）**：

```json
{
  "status": "ok",
  "bitable_mode": "real",
  "feishu_configured": true,
  "bitable_configured": true,
  "mock_auth_enabled": false,
  "bitable_live": true,
  "bitable_counts": { "materials": 5 },
  "feishu_im": { "ok": true, "bot_in_chat": true }
}
```

**静态页 500 修复方向**（待用户执行）：

1. `chmod 755` 开放 `/home/stock` 至 `dist` 的路径，或
2. 将 `dist` 复制到 `/var/www/stock-flow` 并改 Nginx `root`

**下一步（阻塞飞书端到端验收）**：

1. `curl -sI http://127.0.0.1:8080/` 返回 **200**
2. `ngrok http 8080`（先 unset 代理环境变量）
3. 飞书开放平台三处 URL + 服务器 `backend/.env` 同步 ngrok 域名
4. `sudo systemctl restart stock-flow`
5. 飞书工作台打开 H5：免登 → 搜索 → 出库/入库 UAT

---

## 四、最终技术架构与 MVP API（截至 2026-06-12）

```text
飞书工作台
    └── 网页应用（React H5）     ← 用户操作界面
            └── FastAPI            ← 免登、鉴权、业务、Bitable API
                    └── 飞书多维表格  ← 唯一数据库（五表）
```

**五表模型**：categories、locations、materials、inventory、transactions

**MVP API（8 个）**：

- `GET /auth/feishu/callback`、`GET /auth/jsapi-config`
- `GET /me`、`GET /materials/search`、`GET /materials/{id}`、`GET /materials/{id}/transactions`
- `POST /inbound`（KEEPER+）、`POST /outbound`（ALL）

**实施阶段**：

| 阶段 | 目标 | 工期 |
|------|------|------|
| P0 | fork 模板、五表就绪、飞书应用创建 | 1～2 天 |
| P1 | lark-samples 迁入、8 API、4 H5 页、工作台联调 | 约 1 周 |
| P2 | 扫码、快递暂存上架 | 3～5 天 |

---

## 五、本日产出物清单

| 类型 | 路径 / 链接 | 说明 |
|------|------------|------|
| 产品设计文档 | `docs/产品设计文档.md` v0.8 | 场景、功能、权限、实施计划 |
| 架构设计文档 | `docs/架构设计文档.md` v0.8 | 组合方案 §3.4、部署、安全 |
| Cursor 规则 | `.cursor/rules/stock-project-conventions.mdc` | 强制约定 |
| Agent 入口 | `AGENTS.md` | 开发约定摘要 |
| Wiki 初稿 | [原 Wiki 文档](https://zcnce50wan15.feishu.cn/wiki/Nzw3wX2USiIjtckGQbVckSQRnFf) | 项目起点 |
| 本实录 | [飞书云文档](https://zcnce50wan15.feishu.cn/docx/YJ4iduq3ToSma8xzuvuci9BlnJg) | 分享案例素材 |

---

## 五、两日产出物清单（2026-06-10 ～ 2026-06-11）

### 5.1 文档与规范

| 类型 | 路径 / 链接 | 版本 | 说明 |
|------|------------|------|------|
| 产品设计文档 | `docs/产品设计文档.md` | v0.8 | 场景、功能、权限、MVP 范围 |
| 架构设计文档 | `docs/架构设计文档.md` | v0.8 | 组合方案、部署、安全、开发边界 |
| 环境需求文档 | `docs/环境需求.md` | v1.1 | 系统安装、Python/Node 环境、Bitable/飞书配置、部署准备 |
| 技术日程文档 | `docs/技术准备与开发日程.md` | v1.0 | 精简版日程、今日目标、验收标准、顺延代码开发 |
| 总体设计文档 | `docs/总体设计.md` | v1.0 | 架构概览、组件职责、API 列表、AI 提示词 |
| 模块设计文档 | `docs/模块设计.md` | v1.0 | 后端/前端模块拆解、接口规范、测试要点 |
| AI 提示词文档 | `docs/ai提示词.md` | v1.0 | AI 代码骨架和表格生成提示词 |
| Cursor 规则 | `.cursor/rules/stock-project-conventions.mdc` | 已定 | 持久规则，确保项目约定一致 |

### 5.2 实际产出与配置文件

| 类型 | 路径 | 说明 |
|------|------|------|
| 开发过程实录 | `docs/开发过程实录-飞书同步稿.md` | 对话记录、阶段演进、决策链、最新补充 |
| 权限导入文件 | `feishu_permission_import.json` | 飞书权限配置导入模板（含 IM 群成员读权限） |
| 设计与计划同步 | `docs/技术准备与开发日程.md` | 代码任务顺延、周末避开、今日以文档准备为主 |
| 后端 MVP | `backend/app/` | FastAPI + Bitable real + 飞书免登 |
| 前端 MVP | `frontend/src/` | 4 业务页 + 飞书 H5 鉴权 |
| 服务器配置 | `/etc/nginx/conf.d/stock-flow.conf` | Nginx 8080 + `/api` 反代 |
| systemd 服务 | `/etc/systemd/system/stock-flow.service` | uvicorn 守护进程 |

### 5.3 外部同步资源

| 资源 | 链接 | 说明 |
|------|------|------|
| Wiki 初稿 | [飞书 Wiki](https://zcnce50wan15.feishu.cn/wiki/Nzw3wX2USiIjtckGQbVckSQRnFf) | 产品起点与需求来源 |
| 同步云文档 | [飞书云文档](https://zcnce50wan15.feishu.cn/wiki/UeJrwo3EDiDRcpk6PMBc7Wq3nJh) | 开发过程实录同步目标 |
| Erniu 模板 | [飞书 Bitable](https://ccn1hpzj4iz4.feishu.cn/base/DyAYb1D2RaYcbQsjdsdcZOEOnad) | P0 Bitable 基础模板 |
| lark-samples | [GitHub](https://github.com/larksuite/lark-samples) | 飞书免登 + JSAPI H5 骨架 |

---

## 六、可分享的方法论（案例价值）

### 6.1 我们做对了什么

1. **从已有 Wiki 出发，而非白纸** —— 快速建立共识
2. **每轮对话都落文档** —— PRD/架构版本化，避免口头决策流失
3. **先问「有没有现成的」再写代码** —— 调研 Erniu、应用模式、lark-samples
4. **约束驱动简化** —— 「低访问量、专人维护」砍掉 80% 过度设计
5. **规则固化** —— Cursor Rules 让 AI 后续不跑偏
6. **组合创新而非从零创新** —— 模板 + 脚手架 + 薄自研

### 6.2 决策演进图（分享用）

```text
Wiki 初稿（Bitable + H5 + FastAPI）
    → 评估合适性 + 简化（专人维护）
    → 调研成品（应用模式 / Erniu / lark-samples）
    → 建立 PRD + 架构文档
    → 权限专题
    → 固化 Cursor 规则
    → 明确 MVP = Bitable + 工作台 H5
    → 部署后置不阻塞
    → 组合方案定稿（反造轮子）
```

### 6.3 给类似项目的建议

| 建议 | 说明 |
|------|------|
| 文档即单一事实来源 | 代码未动，文档先行 |
| 飞书生态内优先复用 | 模板、samples、开源 Erniu |
| 自研边界画清楚 | 只写 4 页 + 8 API，不写免登/表结构 |
| AI 协作要「可审计」 | 变更记录 + 项目 Rules |
| 分享案例保留决策链 | 不只展示结果，更要展示「为什么变」 |

---

## 七、待办与下一步

- [x] P0：fork Erniu Bitable 模板，按 PRD 调整五表（real 模式已读写）
- [x] P0：飞书开放平台创建自建应用（网页应用 + bitable 权限）
- [x] P1：迁入 lark-samples 思路，实现 MVP API + 4 H5 页
- [x] P1：本地/服务器 real 模式出入库联调
- [ ] **进行中**：服务器 Nginx 8080 静态页 500 修复（权限或 `/var/www`）
- [ ] **进行中**：ngrok 8080 穿透 + 飞书三处 URL 与 `.env` 同步
- [ ] 飞书工作台端到端 UAT（免登 → 搜索 → 出库 → 入库）
- [ ] 待定：研发归还是否允许自行入库
- [ ] 可选：ngrok 9009 代理问题时改用 cloudflared

---

## 八、变更记录（本实录）

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-06-10 | 首版：汇总 2026-06-10 全天 AI 协作开发过程 |
| v1.1 | 2026-06-11 | 新增环境需求文档整理；补充各阶段日期标注 |
| v1.2 | 2026-06-11 | 文档细化、总体与模块设计、日程同步与矢量图移除 |
| v1.3 | 2026-06-12 | MVP 代码 real 联调、阿里云部署、AutoDL 占 80 排查、8080 + ngrok 方案、health 已通 |

---

*本文档由 Cursor AI 根据项目对话与本地 `stock-test/docs` 文档自动整理，用于团队内部分享与复盘。*
